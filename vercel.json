{
  "buildCommand": "npm install && rm -rf client/node_modules client/package-lock.json && npm --prefix client install --no-audit --no-fund && npm --prefix client rebuild esbuild && npm --prefix client run build",
  "outputDirectory": "client/dist",
  "routes": [
    { "src": "^/api/(.*)$", "dest": "/api/$1" },
    { "src": "^(?!/api/).*$", "dest": "/client/dist/index.html" }
  ]
}
