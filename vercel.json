{
  "buildCommand": "npm install && npm --prefix client install --no-audit --no-fund && npm --prefix client i -D @rollup/rollup-linux-x64-gnu@4.24.0 && npm --prefix client run build",
  "outputDirectory": "client/dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/client/dist/$1" }
  ]
}
