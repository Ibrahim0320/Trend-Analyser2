{
  "buildCommand": "npm ci && npm --prefix client install --no-audit --no-fund && npm --prefix client run build",
  "outputDirectory": "client/dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/client/dist/$1" }
  ]
}
