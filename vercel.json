{
  "buildCommand": "npm install && rm -rf client/node_modules && npm --prefix client ci && npm --prefix client rebuild esbuild && npm --prefix client run build",
  "outputDirectory": "client/dist",
  "routes": [
    { "src": "^/api/(.*)$", "dest": "/api/$1" },
    { "src": "^(?!/api/).*", "dest": "/client/dist/index.html" }
  ]
}
